name: opencode

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  opencode:
    if: |
      github.event_name == 'pull_request' ||
      contains(github.event.comment.body, ' /oc') ||
      startsWith(github.event.comment.body, '/oc') ||
      contains(github.event.comment.body, ' /opencode') ||
      startsWith(github.event.comment.body, '/opencode')
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Run opencode
        uses: anomalyco/opencode/github@latest
        env:
          OPENCODE_API_KEY: ${{ secrets.OPENCODE_API_KEY }}
        with:
          model: opencode/big-pickle
          prompt: |
            이 Pull Request을 검토하고 개선되어야하는 점을 기재해주세요:
              - 코드 품질 이슈를 찾아주세요
              - 잠재적 버그를 찾아주세요
              - react-router-v7 skill을 사용하여 react-router-v7 관련 코드를 검토해주세요
            Comment Template은 다음과 같습니다.

            ## 코드 리뷰 결과

            ### 🔴 Critical Issues

            | 파일 | 이슈 |
            |------|------|
            | `파일경로:라인` | 이슈 내용 |

            ### 🟡 React Router v7 관련

            | 파일 | 이슈 |
            |------|------|
            | `파일경로:라인` | 이슈 내용 |

            ### 🟠 코드 포맷팅

            | 파일 | 이슈 |
            |------|------|
            | `파일경로:라인` | 이슈 내용 |

            ### 🟢 ESLint 설정

            | 파일 | 이슈 |
            |------|------|
            | `파일경로:라인` | 이슈 내용 |

            ---

            ### 수정 권장 사항

            ```bash
            # 필요한 명령어
            ```
